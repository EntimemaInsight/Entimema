# Sprint B — Motion, Scroll & Interaction

## Included
- Compact sticky header after scroll
- Persistent announcement dismissal for the current browser session
- Shared scroll-reveal system with restrained stagger timing
- Refined hover behavior for navigation, service rows, cards and CTA arrows
- Correct anchor offsets below the sticky header
- Mobile-friendly interaction rules
- `prefers-reduced-motion` accessibility support

## Verification
Run:

```powershell
npm install
npm run build
npm run dev
```
