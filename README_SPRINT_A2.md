# ENTIMEMA Sprint A.2 — Editorial Hero Restoration

## Included
- Single primary CTA “Обсъдете казус” in the header.
- Restored original Hero statement: “Структурата превръща стратегията в резултати.”
- Restored the five core directions on the right side of the opening screen.
- No product image or product preview in the opening screen.
- “Как работим” is not displayed.
- Remaining visible English labels in the finance/risk cards are localised into Bulgarian.

## Install
Copy the contents of the `entimema` folder over your local project, then run:

```powershell
cd C:\Users\user\entimema
npm install
npm run dev
```

After review:

```powershell
git add .
git commit -m "Restore editorial hero and header CTA"
git push origin main
```
