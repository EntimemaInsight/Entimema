ENTIMEMA HOME v0.3

1. Copy the folder "app" into C:\Users\user\entimema
2. Choose "Replace the file in the destination" for app\globals.css
3. Keep npm run dev running and refresh localhost:3000
4. When approved, publish:
   git add .
   git commit -m "Home v0.3 responsive refinement"
   git push
