import { createHmac,randomUUID,timingSafeEqual } from "node:crypto";
import type { InspectedDocument } from "../lib/files";
import { runInspectedDocumentClassifier } from "../api/agents/document-classifier/run";
import { extract } from "./extraction";
import { CANONICAL_INCOME_STATEMENT_VERSION,type FinancialRun,type ReviewDecision,type ReviewTask } from "./schema";
import { evaluateReadiness,summarizeValidation,validate } from "./validation";
const integritySecret=()=>process.env.AUTH_SECRET??process.env.NEXTAUTH_SECRET??"entimema-development-review-integrity";
function signRun(run:FinancialRun){const {integrity,...payload}=run;void integrity;return createHmac("sha256",integritySecret()).update(JSON.stringify(payload)).digest("hex")}
export function hasValidIntegrity(run:FinancialRun){const expected=signRun(run),received=run.integrity??"";return expected.length===received.length&&timingSafeEqual(Buffer.from(expected),Buffer.from(received))}
function tasksFor(run:Pick<FinancialRun,"periods"|"values"|"controls"|"currency"|"unitScale">,selected:boolean,imageOnly=false):ReviewTask[]{const tasks:ReviewTask[]=[];const add=(t:ReviewTask)=>tasks.push(t);if(imageOnly)add({id:"review-image",groupKey:"structure:image",issueType:"image_only_pdf",valueId:null,sourceRowId:null,sourceLabel:"Image-only PDF",sourceValue:null,proposedConcept:null,confidence:0,evidenceId:null,controlIds:[],recommendedAction:"Provide a text-based PDF or spreadsheet. OCR is not enabled.",material:true,state:"open"});if(!selected)add({id:"review-statement",groupKey:"statement",issueType:"ambiguous_statement",valueId:null,sourceRowId:null,sourceLabel:"Income Statement section",sourceValue:null,proposedConcept:null,confidence:0,evidenceId:null,controlIds:[],recommendedAction:"Provide a clearly labelled English Income Statement.",material:true,state:"open"});const unknown=run.periods.filter(p=>p.reviewRequired);if(unknown.length)add({id:"review-periods",groupKey:"periods",issueType:"ambiguous_period",valueId:null,sourceRowId:null,sourceLabel:unknown.map(p=>p.originalHeader).join(", "),sourceValue:null,proposedConcept:null,confidence:Math.min(...unknown.map(p=>p.confidence)),evidenceId:null,controlIds:[],recommendedAction:"Confirm or correct these source period headers.",material:true,state:"open"});const byRow=new Map<string,typeof run.values>();for(const v of run.values.filter(v=>v.reviewState==="required"))byRow.set(v.sourceRowId,[...(byRow.get(v.sourceRowId)??[]),v]);for(const [rowId,values] of byRow){const v=values[0];add({id:`review-mapping-${rowId}`,groupKey:`mapping:${rowId}`,issueType:"low_confidence_mapping",valueId:v.id,sourceRowId:rowId,sourceLabel:v.sourceLabel,sourceValue:null,proposedConcept:v.concept,confidence:v.mappingConfidence,evidenceId:v.evidenceId,controlIds:[],recommendedAction:`Choose one mapping for all ${values.length} period values on this source row.`,material:true,state:"open"})}if(run.values.length&&!run.currency)add({id:"review-currency",groupKey:"currency",issueType:"unknown_currency",valueId:null,sourceRowId:null,sourceLabel:"Reporting currency",sourceValue:null,proposedConcept:null,confidence:0,evidenceId:null,controlIds:[],recommendedAction:"Confirm the reporting currency.",material:true,state:"open"});if(run.values.length&&!run.unitScale)add({id:"review-scale",groupKey:"scale",issueType:"unknown_scale",valueId:null,sourceRowId:null,sourceLabel:"Unit scale",sourceValue:null,proposedConcept:null,confidence:0,evidenceId:null,controlIds:[],recommendedAction:"Confirm units, thousands, or millions.",material:true,state:"open"});for(const c of run.controls.filter(c=>c.reviewRequired))add({id:`review-${c.id}`,groupKey:`control:${c.id}`,issueType:c.id==="duplicate-mapping"?"duplicate_mapping":c.id==="evidence-completeness"?"missing_evidence":"failed_reconciliation",valueId:null,sourceRowId:null,sourceLabel:c.formula,sourceValue:c.reportedValue,proposedConcept:null,confidence:0,evidenceId:c.affectedEvidence[0]??null,controlIds:[c.id],recommendedAction:"Inspect source values and mappings; resolve the failed control.",material:true,state:"open"});return tasks}
function finalize(base:Omit<FinancialRun,"status"|"readiness"|"validationSummary"|"confidence"|"workflow"|"integrity">,statementConfidence:number,imageOnly=false):FinancialRun{const validationSummary=summarizeValidation(base.controls,base.periods.length);let readiness=evaluateReadiness({selected:Boolean(base.source.selectedSection),statementConfidence,periods:base.periods,values:base.values,evidence:base.evidence,currency:base.currency,unitScale:base.unitScale,controls:base.controls,tasks:base.reviewTasks,unsupported:imageOnly});if(!readiness.gates.coverage&&!base.reviewTasks.some(t=>t.groupKey==="validation-coverage")){base.reviewTasks.push({id:"review-validation-coverage",groupKey:"validation-coverage",issueType:"unsupported_structure",valueId:null,sourceRowId:null,sourceLabel:"Validation coverage",sourceValue:null,proposedConcept:null,confidence:validationSummary.coverage,evidenceId:null,controlIds:[],recommendedAction:"Map or provide the prerequisite anchor lines needed to validate reported profit subtotals.",material:true,state:"open"});readiness=evaluateReadiness({selected:Boolean(base.source.selectedSection),statementConfidence,periods:base.periods,values:base.values,evidence:base.evidence,currency:base.currency,unitScale:base.unitScale,controls:base.controls,tasks:base.reviewTasks,unsupported:imageOnly})}const mapped=base.values.filter(v=>v.concept!=="other_reported_line").length;const periodDetection=base.periods.length?base.periods.reduce((n,p)=>n+p.confidence,0)/base.periods.length:0;const confidence={statementDetection:statementConfidence,periodDetection,currencyDetection:base.currency?1:0,scaleDetection:base.unitScale?1:0,sourceValueExtraction:base.values.length?.95:0,canonicalMapping:base.values.length?mapped/base.values.length:0,evidenceCompleteness:base.values.every(v=>base.evidence.some(e=>e.id===v.evidenceId))?1:0,deterministicReconciliation:validationSummary.coverage*validationSummary.passRate};const workflow:FinancialRun["workflow"]=[{id:"intake",label:"File Intake",state:"completed"},{id:"classification",label:"Document Classification",state:"completed"},{id:"detection",label:"Statement Detection",state:base.source.selectedSection?"completed":"blocked"},{id:"extraction",label:"Income Statement Extraction",state:base.values.length?"completed":"blocked"},{id:"mapping",label:"Canonical Mapping",state:base.reviewTasks.some(t=>t.issueType==="low_confidence_mapping")?"review_required":"completed"},{id:"validation",label:"Financial Validation",state:validationSummary.failed?"blocked":validationSummary.coverage<.75?"warning":"completed"},{id:"review",label:"Human Review",state:readiness.status==="review_required"||base.reviewTasks.some(t=>t.state==="open")?"review_required":"completed"},{id:"result",label:"Validated Result",state:readiness.status==="validated"?"completed":"blocked"}];const result:FinancialRun={...base,integrity:"",status:readiness.status,readiness,validationSummary,confidence,workflow};result.integrity=signRun(result);return result}
export async function runFinancialIntelligence(document:InspectedDocument):Promise<FinancialRun>{const classified=await runInspectedDocumentClassifier(document),extracted=await extract(document),controls=validate(extracted.values,extracted.evidence,extracted.scale);const shell={runId:randomUUID(),schemaVersion:CANONICAL_INCOME_STATEMENT_VERSION,sessionScoped:true as const,classification:classified.response,detectedStatements:[...new Set([classified.response.classification.document_type,...extracted.inventory.filter(x=>/balance|cash|income|profit/i.test(x))])],source:{filename:document.fileName,format:document.extension,selectedSection:extracted.selected,inventory:extracted.inventory},rows:extracted.rows,periods:extracted.periods,currency:extracted.currency,unitScale:extracted.scale,values:extracted.values,evidence:extracted.evidence,controls,reviewTasks:[] as ReviewTask[]};shell.reviewTasks=tasksFor(shell,Boolean(extracted.selected),Boolean("imageOnly" in extracted&&extracted.imageOnly));return finalize(shell,extracted.selected?1:0,Boolean("imageOnly" in extracted&&extracted.imageOnly))}
export function replayFinancialReview(run:FinancialRun,decision:ReviewDecision):FinancialRun{if(!hasValidIntegrity(run))throw new Error("Invalid run integrity");const task=run.reviewTasks.find(t=>t.id===decision.taskId&&t.state==="open");if(!task)throw new Error("Unknown or resolved review task");let currency=run.currency,unitScale=run.unitScale,periods=run.periods,values=run.values;if(decision.action==="confirm_currency"&&decision.currency)currency=decision.currency.toUpperCase();else if(decision.action==="confirm_scale"&&decision.unitScale)unitScale=decision.unitScale;else if(task.sourceRowId){values=values.map(v=>v.sourceRowId!==task.sourceRowId?v:{...v,concept:decision.concept??v.concept,originalProposal:v.originalProposal??v.concept,mappingMethod:"human-validated",mappingConfidence:1,reviewState:decision.action==="reject"?"rejected":"accepted",humanDecision:decision.action})}else if(decision.action==="confirm_period"&&decision.periodLabel)periods=periods.map(p=>p.reviewRequired?{...p,label:decision.periodLabel!,reviewRequired:false,confidence:1}:p);else if((decision.action==="exclude_from_control"||decision.action==="include_in_control")&&task.valueId)values=values.map(v=>v.id===task.valueId?{...v,excludedFromControls:decision.action==="exclude_from_control"}:v);const controls=validate(values,run.evidence,unitScale),shell:FinancialRun={...run,currency,unitScale,periods,values,controls,reviewTasks:[]};shell.reviewTasks=tasksFor(shell,Boolean(run.source.selectedSection));const {integrity,...unsigned}=shell;void integrity;return finalize(unsigned,run.confidence.statementDetection)}
