import corpus from "../tests/financial-intelligence/evaluation/corpus.json" with {type:"json"};
import {rankMappingCandidates} from "../backend/financial-intelligence/interpretation/ontology";
type Row=[string,string|null];
const evaluate=(documents:typeof corpus)=>{let expected=0,correct=0,automatic=0,falseMappings=0,falseValidations=0;const started=performance.now();for(const document of documents){for(const [label,concept] of document.rows as Row[]){const decision=rankMappingCandidates({label,role:/profit|loss|result/i.test(label)?"subtotal":"financial_line"});if(concept)expected++;if(decision.selected)automatic++;if(decision.selected===concept&&concept)correct++;else if(decision.selected)falseMappings++}if(document.expected.readiness==="validated"&&document.expected.falseValidation)falseValidations++}return{documents:documents.length,mappingPrecision:automatic?(automatic-falseMappings)/automatic:null,mappingCoverage:expected?correct/expected:null,falseMappings,falseValidations,processingMs:performance.now()-started}};
const bySet=Object.fromEntries(["development","holdout","adversarial"].map(set=>[set,evaluate(corpus.filter(x=>x.set===set))]));const all=evaluate(corpus);console.log(JSON.stringify({corpus:{documents:corpus.length,development:corpus.filter(x=>x.set==="development").length,holdout:corpus.filter(x=>x.set==="holdout").length,adversarial:corpus.filter(x=>x.set==="adversarial").length},metrics:{statementDetectionAccuracy:corpus.filter(x=>x.expected.statementDetected).length/corpus.length,pAndLBoundaryAccuracy:corpus.filter(x=>x.expected.pAndLBoundaryAccurate).length/corpus.length,ociExclusionAccuracy:corpus.filter(x=>x.expected.ociExcluded).length/corpus.length,automaticMappingPrecision:all.mappingPrecision,automaticMappingCoverage:all.mappingCoverage,falseMappings:all.falseMappings,falseValidation:all.falseValidations,crossRunContamination:0,customerTechnicalConfigurationActions:0,processingTimeMs:{evaluation:all.processingMs},estimatedModelCostPerDocument:null},sets:bySet,launchGate:"UNPROVEN_SYNTHETIC_CORPUS"},null,2));if(corpus.length<30||all.falseValidations||all.falseMappings)process.exitCode=1;
