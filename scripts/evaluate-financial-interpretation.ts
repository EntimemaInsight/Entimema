import corpus from "../tests/financial-intelligence/evaluation/corpus.json" with {type:"json"};
import {rankMappingCandidates} from "../backend/financial-intelligence/interpretation/ontology";
type Row=[string,string|null];let expectedMappings=0,correctMappings=0,autoMappings=0,falseAutoMappings=0,falseValidation=0;
const documents=corpus.map(document=>{let expected=0,correct=0,auto=0,falseAuto=0;for(const [label,concept] of document.rows as Row[]){const decision=rankMappingCandidates({label,role:/profit|loss|result/i.test(label)?"subtotal":"financial_line"});if(concept){expected++;expectedMappings++;if(decision.selected===concept){correct++;correctMappings++}}if(decision.selected){auto++;autoMappings++;if(decision.selected!==concept){falseAuto++;falseAutoMappings++}}}const validated=document.expected.readiness==="validated";if(validated&&document.expected.falseValidation===true){falseValidation++}return{id:document.id,set:document.set,expectedMappings:expected,correctMappings:correct,autoMappings:auto,falseAutoMappings:falseAuto,readiness:document.expected.readiness}});
const result={corpus:{documents:corpus.length,development:corpus.filter(x=>x.set==="development").length,holdout:corpus.filter(x=>x.set==="holdout").length,adversarial:corpus.filter(x=>x.set==="adversarial").length},documents,aggregate:{canonicalMappingPrecision:{numerator:correctMappings,denominator:autoMappings,value:autoMappings?correctMappings/autoMappings:null},canonicalMappingCoverage:{numerator:correctMappings,denominator:expectedMappings,value:expectedMappings?correctMappings/expectedMappings:null},falseAutoMapping:{numerator:falseAutoMappings,denominator:autoMappings,value:autoMappings?falseAutoMappings/autoMappings:null},falseValidationCount:falseValidation},launchGate:"UNPROVEN_SMALL_CORPUS"};console.log(JSON.stringify(result,null,2));if(falseValidation)process.exitCode=1;
