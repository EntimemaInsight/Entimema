# 07 Hero

The hero communicates one promise and five expertise pillars. On standard desktop viewports, the entire composition must be visible without requiring the user to scroll to understand it. Sticky behavior may be used only when subsequent content exists and must degrade naturally on mobile.
