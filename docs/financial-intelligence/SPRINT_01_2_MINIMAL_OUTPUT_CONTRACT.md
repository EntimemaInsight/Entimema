# Sprint 01.2 — Minimal output contract / Nano validation recovery

Date: 2026-09-07T08:24:04.251Z. Branch: codex/fi-v1-hard-reset. Draft PR #174 remains unmerged.

## Result

The first controlled live Nano attempt passed through executeV1, the same core invoked by the production HTTP handler. The acceptance transport uses the real OpenAI Responses SDK with the production request body, abort signal, zero SDK retries and one attempt; no AI mock is used. HTTP 200; requested and resolved model gpt-4.1-nano-2025-04-14; reasoning omitted; exactly one request. The accepted pinned model is now the production default (FI_V1_MODEL remains an explicit override).

## Minimal contract and sizes

The model returns only statementType, entity, currency, scale, periods and lines. Each line contains sourceRow, label, concept and values containing period/sourceRef only. Numeric value, summary and findings were removed from model output. The customer result still includes source-bound numbers, deterministic KPIs, summary and findings.

| Measurement | Previous | New |
|---|---:|---:|
| Document characters | 674 | 674 |
| Instruction characters | 1492 | 1407 |
| JSON format/schema characters | 1345 | 1162 |
| Total input characters | 3511 | 3243 |
| Estimated input tokens (ceil chars/4) | 878 | 811 |
| Actual provider input tokens | 898 | 857 |
| Actual output tokens | 657 | 509 |

These are comparable request components and observed token counts, not a controlled causal latency claim. The previous completed Nano response failed validation; its precise rejection was not retained.

## Privacy-safe diagnostics

ValidationFailure now retains validationFailureCode, failedPath, missingRequiredFields, expectedType, receivedType, category, jsonParsingSucceeded and schemaValidationSucceeded. Categories distinguish schema/contract from source association/value and statement selection. Codes distinguish invalid JSON/enums/types, missing fields, unexpected fields, malformed/missing references, duplicate lines/references/periods, row/label/sheet mismatch and period-header mismatch. HTTP operational logs retain only these structural diagnostics and timing metadata. Unknown field names are redacted. No raw prompt, financial response body or credential is logged.

The live run completed JSON parsing and strict schema validation successfully; no rejection diagnostic was generated. Tests exercise malformed JSON, absent currency, invalid enum/type, extra authoritative numeric values, redacted unknown keys, invalid/missing/non-numeric source refs, duplicates and period/label mismatches. No claim is made about the unknown cause of the earlier Sprint 01.1 rejection.

## Source binding and gold output

Metadata: income_statement; entity null (unstated); currency EUR; scale thousands; periods 2025, 2024. Nine correct semantic lines, eighteen exact source values verified.

| Financial line | 2025 | 2024 | Source references |
|---|---:|---:|---|
| Revenue | 1200 | 1000 | 'P&L'!B5 / 'P&L'!C5 |
| Cost of Sales | -720 | -650 | 'P&L'!B6 / 'P&L'!C6 |
| Gross Profit | 480 | 350 | 'P&L'!B7 / 'P&L'!C7 |
| Operating Expenses | -250 | -220 | 'P&L'!B8 / 'P&L'!C8 |
| Operating Profit | 230 | 130 | 'P&L'!B9 / 'P&L'!C9 |
| Finance Costs | -30 | -20 | 'P&L'!B10 / 'P&L'!C10 |
| Profit Before Tax | 200 | 110 | 'P&L'!B11 / 'P&L'!C11 |
| Income Tax | -50 | -28 | 'P&L'!B12 / 'P&L'!C12 |
| Net Income | 150 | 82 | 'P&L'!B13 / 'P&L'!C13 |

Code resolves each qualified sourceRef to the mechanically read numeric cell before verification. The unchanged verification predicates then check row/label/sheet association, exact numeric equality, period headers and unique references. Invalid associations fail closed. The model cannot supply an authoritative financial number: strict schema validation rejects an extra value field. Reader and calculate.ts remain unchanged; verifier changes attach safe reasons to existing rejection rules.

## Deterministic first analysis

Revenue growth: 20%. Gross margin: 40% / 35%; operating margin: 19.17% / 13%; net margin: 12.5% / 8.2% (2025 / 2024). Added operating profit growth: 76.92%; net income growth: 82.93%. Existing calculation functions are preserved; firstAnalysis adds the two movements and concise observations.

- Revenue increased by 20% in 2025 versus 2024.
- Operating profit increased by 76.92% in 2025 versus 2024.
- Net income increased by 82.93% in 2025 versus 2024.
- Gross margin moved from 35% to 40% in 2025 (+5 pp).
- Operating margin moved from 13% to 19.17% in 2025 (+6.17 pp).
- Net margin moved from 8.2% to 12.5% in 2025 (+4.3 pp).

Summary: Revenue increased by 20% in 2025 versus 2024. Operating profit increased by 76.92% in 2025 versus 2024.

These observations are generated by code from verified values, with no second AI request.

## Live latency and attempts

| Stage | Milliseconds |
|---|---:|
| mechanicalReadMs | 15.22 |
| aiMs | 5757.44 |
| validationMs | 4.42 |
| verificationMs | 2.42 |
| calculationMs | 102.14 |
| totalMs | 5881.71 |

Attempt 1: HTTP 200, PASS. One observation only: median AI latency 5757.44 ms and median total 5881.71 ms. No retry was needed. The production AI budget remains 7000 ms with the unchanged 10-second total ceiling. No timeout increase. Controlled Sprint 01.2 history is capped at three attempts when FI_ACCEPTANCE_SESSION=sprint-01.2; normal acceptance remains reusable.

Machine-readable evidence: [latest acceptance](./SPRINT_01_ACCEPTANCE.json) and [controlled attempts](./SPRINT_01_2_ACCEPTANCE_ATTEMPTS.json).

## Quality gates

- npm run typecheck: PASS.
- npm run lint: PASS, zero errors; one pre-existing unused _ignored warning in backend/agents/document-classifier/validator.ts:8.
- npx tsx --test tests/financial-intelligence/*.test.ts: PASS, 19/19.
- npm run build: PASS, 126 generated pages, production FI route included.
- git diff --check: PASS.

## Preserved boundaries and limitations

ZERO Finance Domain dependency; upload-only UX; same HTTP core; same reader, deterministic base calculations and source verification rules. Authentication, upload controls, rate limiting, transport, database records/migrations and the separate classifier remain preserved. No legacy parser, mapping, selectedSheet, orchestration or second call was introduced. The 35-file retirement remains as documented in the Sprint 01 manifest.

One successful gold sample does not prove stable production latency or accuracy across arbitrary files. Metadata and concepts still require model interpretation. Source grounding does not establish semantic completeness for arbitrary statements. OCR is unsupported; large/ambiguous sources fail closed; PDF text has limited column semantics. Annual movement requires consecutive year labels and positive prior denominators. This is real core acceptance, not an authenticated browser session. Credentials were never printed, logged or committed.

V1_CORE_ACCEPTANCE_PASS
